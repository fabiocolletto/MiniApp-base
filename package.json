{
  "name": "miniapp",
  "version": "3.0.0",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "lint": "npm run lint:eslint && npm run lint:prettier",
    "lint:eslint": "eslint src tests --ext .js",
    "lint:prettier": "prettier --check ."
  },
  "devDependencies": {
    "@types/node": "^22.1.0",
    "eslint": "^9.6.0",
    "eslint-config-prettier": "^9.1.0",
    "firebase": "^10.12.3",
    "jsdom": "^27.2.0",
    "prettier": "^3.3.2",
    "vitest": "^1.6.0"
  }
}
